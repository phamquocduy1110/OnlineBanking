<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tu.thesis.onlinebanking.pk">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/final_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/final_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".FingerprintAuth"
            android:label="@string/app_name"
            android:theme="@style/LoginTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="Home" />
        <activity android:name=".Register" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".Profile"
            android:label="@string/profile" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".Transfer" />
        <activity android:name=".Transfer_Confimed" />
        <activity android:name=".PhoneOperator" />
        <activity android:name=".Bill_Detail_act"></activity>
    </application>

</manifest>